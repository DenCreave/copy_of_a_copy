package copyme.field;

/**
 * own class to background can have its own
 */
public class Background extends MyMap {

    public Background(int x, int y, String defaultPicPath){
        super(x,y,defaultPicPath);
    }
}
